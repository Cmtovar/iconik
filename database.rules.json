{
  "rules": {
    ".read": false,
    ".write": false,
    "backups": {
      "default": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['data', 'updatedAt']) && newData.child('updatedAt').isNumber()"
      }
    }
  }
}
